<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.multicampus.hhh.mapper.AccBoardMapper">

    <!--판매글 등록-->
    <insert id="insert" parameterType="com.multicampus.hhh.dto.AccBoardDTO">
        insert into bike_board(user_id,product_name, image, price,amount,info,brand_name)
        values (#{userid}, #{productname}, #{image}, #{price},#{amount}, #{info}, #{brandname})
    </insert>

    <!--구매 게시판 목록 조회-->
    <select id="selectAll" resultType="com.multicampus.hhh.dto.AccBoardDTO">
        select * from bike_board order by bike_id desc
    </select>

    <!--특정 게시글 조회-->
    <select id="selectOne">
        select * from acc_board where ac_id = #{acid}
    </select>
    <!--게시글 수정(관리자만 가능)-->
    <update id="modify">
        update acc_board set
    </update>
    <!--게시글 삭제(관리자만 가능)-->
    <delete id = "delete">
        delete from acc_board where
    </delete>


</mapper>