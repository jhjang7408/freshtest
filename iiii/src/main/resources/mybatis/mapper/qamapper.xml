<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.multicampus.hhh.mapper.qaBoardMapper">	<!-- 이 매퍼가 어떤 인터페이스와 관련된 매퍼인지 알려줌 -->

    <insert id="insert">
        insert into qa_board(user_id, title, content) values (#{param1}, #{param2}, #{param3})
    </insert>

    <select id="selectAll" resultType="com.multicampus.hhh.domain.qaBoardVO">
        select * from qa_board order by qa_id desc
    </select>

    <select id="selectOne" resultType="com.multicampus.hhh.domain.qaBoardVO">
        select * from qa_board where qa_id = #{_id}
    </select>

    <delete id="delete">
        delete from qa_board where qa_id = #{_id}
    </delete>

    <update id="update">
        update qa_board set title=#{title}, content=#{content} where qa_id=#{qa_id}
    </update>


    <select id="selectList" resultType="com.multicampus.hhh.domain.qaBoardVO">
        select * from qa_board
        <where>
            <if test="types != null and types.length > 0">
                <foreach collection="types" item="type" open="(" close=")" separator=" OR ">
                    <if test="type == 't'.toString()">
                        title like concat('%', #{keyword}, '%')
                    </if>
                    <if test="type == 'w'.toString()">
                        writer like concat('%', #{keyword}, '%')
                    </if>
                </foreach>
            </if>

            <if test="finished">
                <trim prefix="and">
                    finished = 1
                </trim>
            </if>

            <if test="from != null and to != null">
                <trim prefix="and">
                    dueDate between #{from} and #{to}
                </trim>
            </if>

        </where>

        order by qa_id desc LIMIT #{size} OFFSET #{skip}
    </select>

    <select id="getCount" resultType="int">
        select count(qa_id) from qa_board
    </select>

</mapper>