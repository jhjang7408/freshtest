<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.multicampus.springboot.dao.ISimpleBbsDao">	<!-- 이 매퍼가 어떤 인터페이스와 관련된 매퍼인지 알려줌 -->

    <select id="listDao" resultType="com.multicampus.springboot.dto.SimpleBbsDto">
        select * from simple_bbs  order by id desc
    </select>

    <select id="viewDao" resultType="com.multicampus.springboot.dto.SimpleBbsDto">
        select * from simple_bbs where id = #{0}
    </select>

    <insert id="writeDao">
        insert into simple_bbs(id, writer, title, content) values (simple_bbs_seq.nextval, #{param1}, #{param2}, #{param3})
    </insert>

    <delete id="deleteDao">
        delete from simple_bbs where id = #{_id}
    </delete>

</mapper>